import pandas as pd
import numpy as np

df = pd.DataFrame(np.random.randn(10,4))

def color_numbers(val):
    if val < 0:
        return 'color: red'
    else:
        return 'color: black'

styled = df.style.applymap(color_numbers)

print(df)
styled
